#include<stdio.h>
int main ()
{
   //increment operator
   //i++(post increment)
   //++i(pre increment)
   int i=1;
   // printf("%d\n",i++); //first use i value and then increase
   // printf("%d\n",i);
   printf("%d\n",++i);  //first increase i value,then use
   printf("%d\n",i);
   return 0;
   //same for decrement operator only + changes to -
}
